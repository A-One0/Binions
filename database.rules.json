{
  "rules": {
    "pokerTables": {
      "$tableId": {
        ".read": false,
        ".write": false
      }
    },

    "plrAccounts": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    }
  }
}